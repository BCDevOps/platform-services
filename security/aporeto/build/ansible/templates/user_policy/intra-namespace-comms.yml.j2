apiVersion: secops.pathfinder.gov.bc.ca/v1alpha1
kind: NetworkSecurityPolicy
metadata:
  name: intra-namespace-comms-{{ item }}
spec:
  description: |
    allow {{ item }} pods to talk to each other
  source:
    - - $namespace={{ item }}
  destination:
    - - $namespace={{ item }}