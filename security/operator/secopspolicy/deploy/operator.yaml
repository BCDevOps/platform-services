apiVersion: apps/v1
kind: Deployment
metadata:
  name: secopspolicy-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: secopspolicy-operator
  template:
    metadata:
      labels:
        name: secopspolicy-operator
    spec:
      serviceAccountName: secopspolicy-operator
      containers:
        - name: ansible
          command:
          - /usr/local/bin/ao-logs
          - /tmp/ansible-operator/runner
          - stdout
          # Replace this with the built image name
          image: "jasoncleach/operator:latest"
          imagePullPolicy: "Always"
          volumeMounts:
          - mountPath: /tmp/ansible-operator/runner
            name: runner
            readOnly: true
        - name: operator
          # Replace this with the built image name
          image: "jasoncleach/operator:latest"
          imagePullPolicy: "Always"
          volumeMounts:
          - mountPath: /tmp/ansible-operator/runner
            name: runner
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "secopspolicy-operator"
      volumes:
        - name: runner
          emptyDir: {}
