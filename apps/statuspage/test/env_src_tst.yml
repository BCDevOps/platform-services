- hosts: localhost
  connection: local
  tasks: 
    # - name: read env var
    #   shell: ". {{ playbook_dir }}/environ"
    # - name: output
    #   debug: msg="{{ lookup('env', 'BLABLA') }}"
    - debug: msg="{{ lookup('file',  '../grafana_creds') | regex_search('^GF_AUTH_OAUTH_CLIENT_SECRET.*', multiline=True) | regex_search('[^=]*$') }}"
    - debug: msg="{{ lookup('file',  '../grafana_creds') | regex_search('^GF_AUTH_OAUTH_AUTH_URL.*', multiline=True) | regex_search('[^=]*$') }}"
