{   
  "canUseAwsMetrics": false,
  "canUseCustomEvents": false,
  "canUseSysdigCapture": false,
  "description": "{{ team.description }}",
  "name": "{{ container_team_name }}",
  "id": {{ container_team_data.id | int }},
  "version": {{ container_team_data.version | int }},
  "show": "container",
  "theme": "#73A1F7",
  "filter": "kubernetes.namespace.name in ({% for namespace in namespaces  %}\"{{ namespace }}\"{% if not loop.last %},{% endif %}{% endfor %} )",
  "userRoles": [
{% for role in team_users_and_roles  %}
    {
      "userId": {{ role.userID }},
      "role": "{{ role.role }}"
    }
{% if not loop.last %}
    ,
{% endif %}
{% endfor %}     
  ]
}